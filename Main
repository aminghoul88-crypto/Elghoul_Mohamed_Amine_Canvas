<!DOCTYPE html>
<html lang="fr">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>DrawApp</title>
<style>
* { box-sizing: border-box; margin: 0; padding: 0; }

body {
    font-family: Arial;
    background: #3a3a5c;
    color: white;
}

header {
    padding: 15px;
    font-size: 22px;
    font-weight: bold;
    background: #2a2a4c;
}

.container {
    display: flex;
    height: 70vh;
    margin: 10px;
}

.canvas-container {
    flex: 1;
    background: #fff;
    border-radius: 10px;
    overflow: hidden;
}

canvas {
    width: 100%;
    height: 100%;
    display: block;
    cursor: crosshair;
}
</style>
</head>
<body>

<header>🎨 DrawApp</header>

<div class="container">
    <div class="canvas-container">
        <canvas id="canvas"></canvas>
    </div>
</div>

<script>
const canvas = document.getElementById("canvas");
const ctx = canvas.getContext("2d");

// Redimensionner le canvas à la taille réelle du conteneur
function resizeCanvas() {
    canvas.width = canvas.offsetWidth;
    canvas.height = canvas.offsetHeight;
}

resizeCanvas();
window.addEventListener("resize", resizeCanvas);
</script>
</body>
</html>
